[project]
name = "dms-halfling"
version = "0.1.0"
description = "A tool to assist DMs with running PF2e Society games."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymupdf>=1.26.3",
]

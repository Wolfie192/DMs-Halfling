import json
import os
import src.tools.pdf as pdf
import src.config as config
from src.config._imported_modules import IMPORTED_MODULES


def main():
	'''
	pdfs_dir = "D:\\ProgrammingProjects\\Python\\DMs-Halfling\\pdfs_for_upload_testing"
	
	for root, _, files in os.walk(pdfs_dir):
		for file in files:
			file_path = os.path.join(root, file)
			pdf.extract_pdf.run(file_path)
	'''

	
main()
